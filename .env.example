PORT=3000
JWT_SECRET=change_this_secret
EMAIL_HOST=smtp.ethereal.email
EMAIL_PORT=587
EMAIL_USER=your_ethereal_user
EMAIL_PASS=your_ethereal_password
OWNER_EMAIL=owner@example.com
DATABASE_URL=
# Example Supabase DATABASE_URL (set this in Vercel or Railway environment variables):
# postgres://<db_user>:<db_pass>@db.<project>.supabase.co:5432/postgres
# For production (Vercel) ensure your DB allows SSL and set NODE_ENV=production so the server enables TLS.